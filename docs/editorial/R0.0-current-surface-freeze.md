# R0.0 — Current Surface Freeze

Status: **MATERIALIZED / AWAITING CI WITNESS**

## Purpose

Freeze the current public Portfolio before any editorial ontology or visual migration begins. R0.0 answers one question only:

> What exactly is public today, and what must a later migration reconcile without silently changing meaning?

This cut is descriptive, not normative. It records the current world before R0.1 defines the constitution of the next one.

## Canonical baseline

- Repository: `4LFR3Dv1/Portfolio`
- Branch: `main`
- Commit: `bff5519fd2b4986dd0c176bc96974b3233d97525`
- Captured: `2026-08-30`
- Public origin: `https://renan.snelabs.space`

The baseline commit is the merge of PR #6, `Promote the header to primary navigation`.

## Frozen public route family

The current runtime recognizes:

- `/`
- `/architecture`
- `/work/vira`
- `/work/xs-wallet`
- `/work/agentic-systems`
- `/work/transactional-support-bot`
- `/work/sne-os`
- `/work/verify-systems`

Unknown application paths currently fall back to the landing projection. That behavior is part of the observed baseline, not an endorsement for the editorial runtime.

## Frozen project identities and visibility

| Project | Legacy visibility |
| --- | --- |
| VIRA | `public` |
| XS Wallet | `public` |
| Agentic Systems & Foundry | `private` |
| Transactional Support Bot | `case-study` |
| SNE OS | `public` |
| VERIFY SYSTEMS | `public` |

`case-study` is intentionally preserved as a legacy value. R0.6 will decide how it decomposes into disclosure, source visibility and evidence visibility.

## Frozen project guarantees

The current project catalog publishes these guarantee labels:

- VIRA — `AUTHORITATIVE STATE`, `DETERMINISTIC REPLAY`, `PRIVATE ANSWERS`, `OBSERVABLE DELIVERY`
- XS Wallet — `SELF-CUSTODY`, `IPC-FIRST`, `LOCAL ENCRYPTION`, `EXPLICIT SESSIONS`
- Agentic Systems & Foundry — `EXPLICIT STATE`, `HUMAN REVIEW`, `TRACEABLE OUTPUTS`, `BOUNDED EXECUTION`
- Transactional Support Bot — `AUTHORITATIVE STATE`, `IDENTITY BINDING`, `IDEMPOTENT HANDOFF`, `EXPIRING SESSIONS`, `PROVIDER ABSTRACTION`, `AUDIT TRAIL`
- SNE OS — `RESPONSIVE UX`, `EXPLICIT CONTEXT`, `PUBLIC DEMO`
- VERIFY SYSTEMS — `VERIFIABLE STATE`, `CONTINUOUS RECONCILIATION`, `EXPLICIT FAILURE MODES`

R0 records these as published claims. It does not yet classify whether each one becomes a future Claim record, design invariant or presentation label.

## Frozen Architecture Explorer

The current architecture catalog contains five views:

1. `systems`
2. `settlement`
3. `agents`
4. `realtime`
5. `handoff`

Its public governing principles include:

- `Intelligence can propose. Deterministic boundaries authorize, execute and verify.`
- `Prepare and simulate → verify → authorize exact bytes → execute → reconcile.`
- `Plan → delegate → inspect → review → merge → preserve evidence.`
- `The browser renders. The server decides. The event trail explains.`
- `Chat is an interface. The backend remains the source of truth.`

The sanitized-public boundary is part of the frozen behavior: credentials, customer data and private deployment topology are intentionally excluded.

## Frozen evidence surface

The Evidence Room currently exposes seven evidence entries:

- VIRA live product
- VIRA readiness endpoint
- Portfolio public source
- EditalSales public source
- ORDM testnet public source
- VERIFY SYSTEMS public PDF
- Renan Melo CV PDF

Project case studies additionally expose project-specific public locators where available, including VIRA evaluation/playback material, XSWallet source, SNE OS and VERIFY SYSTEMS.

Evidence is currently represented in more than one catalog. R0.0 freezes that duplication; R0.4 will define one evidence authority.

## Frozen publication surface

The current Publications section exposes:

- VERIFY SYSTEMS
- Production Systems Handbook
- TypeScript Handbook

All three are implemented directly by the React publication component and resolve to PDF artifacts under `public/docs`.

## Frozen public identity

The current surface presents:

- `RENAN MELO`
- `Blockchain & Agentic Systems Engineer`
- `Author of VERIFY SYSTEMS`
- portfolio-oriented navigation and contact routing

The current web manifest and structured metadata still identify the site as `Renan Melo — Portfolio`.

## Frozen engineering gate

The repository gate is:

```text
npm run typecheck
npm run lint
npm run test
npm run build
```

and `npm run verify` composes those checks. R0 work must preserve this gate.

## Machine-readable witness

`docs/editorial/legacy/portfolio-surface.v0.json` is the R0.0 machine-readable snapshot. `src/app/data/editorial-surface-freeze.test.ts` reconciles the live legacy catalogs and public files against that snapshot.

The manifest is not the future content model. It is a witness of the pre-migration system.

## Non-claims

R0.0 does **not** establish:

- the final editorial ontology;
- the future route structure;
- Astro, MDX or any framework migration;
- the future design language;
- that every legacy guarantee is an established epistemic claim;
- that every public source is production-ready;
- that every public record is fully auditable.

## Acceptance

R0.0 is complete only when:

```text
canonical_baseline_frozen=true
legacy_routes_frozen=true
legacy_project_identity_frozen=true
legacy_visibility_frozen=true
legacy_claim_labels_frozen=true
legacy_architecture_frozen=true
legacy_evidence_locators_frozen=true
legacy_publications_frozen=true
legacy_sitemap_reconciled=true
runtime_semantics_changed=false
ui_changed=false
npm_run_verify=PASS
R0_0_COMPLETE=true
```

Until CI witnesses the branch, `R0_0_COMPLETE=false`.
