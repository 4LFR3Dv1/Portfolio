# R1.4 materialization note

This cut changes only the editorial substrate. It does not modify the deployed React/Vite Portfolio surface.

The initial language state is explicit:

```text
born System Records             28
canonical EN realizations       28
explicit PT-BR translations      5
language bindings total         33
routed language pairs           10
public projections               0
```

The existence of a locale-specific route never constitutes proof that a realization exists for that language. Exact current-revision `identity.language-binding` admission is required.
