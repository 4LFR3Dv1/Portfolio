# R2.3 Materialization Note

The materialized state for R2.3 is the branch state immediately before the terminal completion seal.

It includes:

- frozen-source legacy reconstruction;
- bounded legacy renderer input;
- three legacy case-study pages;
- the five-view legacy Architecture Explorer;
- `portfolio-language` EN/PT compatibility behavior;
- `noindex,follow` distribution quarantine;
- physical shell verification for 18 canonical + 4 historical pages;
- no production or redirect activation.

The terminal completion seal must bind the exact manifest blob and the successful Verify + Editorial Shell Build witnesses produced from this state.
